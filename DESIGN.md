# Tascal デザインシステム

Tascal（タスカル）のビジュアル言語・UI 規約をまとめたドキュメントです。
Claude Design で作成したプロトタイプ `ToDoアプリ.dc.html` を出典とし、実装（React + Vite + TypeScript）が
参照する唯一の正（Single Source of Truth）として運用します。

すべてのトークンは CSS カスタムプロパティとして `src/styles/tokens.css` に定義され、
ダーク / ライトの 2 テーマを切り替えても破綻しないように設計されています。

---

## 1. デザイン原則

| 原則 | 内容 |
| --- | --- |
| **静かな暗色基調** | 既定はダーク。彩度を抑えたサーフェスの階層で奥行きを表現し、コンテンツ（タスク）を主役にする。 |
| **色は意味に使う** | 灰系の UI に対し、彩度の高い色は「プロジェクト」「優先度」「締切」「達成」など“意味”がある箇所だけに使う。 |
| **触れて気持ちいい** | 完了チェックの描画アニメ、達成バースト、行のフェードインなど、操作のフィードバックを軽快なモーションで返す。 |
| **密度は調整可能** | 行の余白（`--row-pad`）をトークン化し、compact / comfortable / roomy を切り替えられる。 |
| **一貫した角丸と境界** | 角丸とごく薄い境界線でカードを面として見せ、影は最小限（ドロワーのみ）。 |

---

## 2. カラートークン

色はすべて CSS 変数で管理します。**ハードコードした 16 進数を新たに増やさない**こと（既存パレットを使う）。

### 2.1 サーフェス（背景の階層）

| トークン | ダーク | ライト | 用途 |
| --- | --- | --- | --- |
| `--bg` | `#14161b` | `#f4f5f8` | アプリ最背面・ドロワー背景 |
| `--sidebar` | `#1a1d24` | `#ebedf2` | サイドバー |
| `--surface` | `#21252e` | `#ffffff` | カード・入力欄・ボタン背景 |
| `--surface-2` | `#262b35` | `#f1f3f7` | カード上のチップ・ピル等の一段手前 |

### 2.2 境界線

| トークン | ダーク | ライト | 用途 |
| --- | --- | --- | --- |
| `--border` | `rgba(255,255,255,.07)` | `rgba(18,22,31,.09)` | 既定の境界 |
| `--border-2` | `rgba(255,255,255,.12)` | `rgba(18,22,31,.16)` | ホバー時・強調された境界 |

### 2.3 テキスト

| トークン | ダーク | ライト | 用途 |
| --- | --- | --- | --- |
| `--text` | `#e9ebf2` | `#1b1e26` | 本文・見出し |
| `--muted` | `#8a90a1` | `#6b7180` | 補助テキスト・非選択ナビ |
| `--faint` | `#5d6373` | `#a6abb8` | セクションラベル・プレースホルダ・最弱の要素 |

### 2.4 アクセント / セマンティック

| トークン | 値 | 意味 |
| --- | --- | --- |
| `--accent` | `#5b8def`（青） | 主アクション・「今日」・選択状態・フォーカス |
| `--accent-soft` | `rgba(91,141,239,.16)`（ライトは `.12`） | アクセントの薄い背景（選択チップ等） |
| `--accent2` | `#42d3a8`（緑） | 達成・完了・進捗（チェック済み、進捗バー、達成リング） |
| `--accent2-soft` | `rgba(66,211,168,.14)` | 緑の薄い背景 |
| `--p-high` | `#f2796d`（赤） | 優先度「高」・破壊的操作（削除・期限超過） |
| `--p-med` | `#f0b95b`（黄） | 優先度「中」・「今後の予定」 |
| `--p-low` | `#7d8597`（灰） | 優先度「低」 |
| `--on-accent` | `#0c1016` | アクセント/緑の塗りの上に乗せる前景色（濃色） |

> **アクセントの前景色**: `--accent` と `--accent2` は明度が高いため、その上のテキストやアイコンには
> 白ではなく濃色 `--on-accent (#0c1016)` を使う。

### 2.5 プロジェクト / タグのパレット

ユーザーが追加するプロジェクトには、以下の 6 色を順番に循環して割り当てます（`src/lib/color.ts`）。

```
#5b8def  #42d3a8  #f0b95b  #c08bf0  #f2796d  #5ed3e0
```

タグの「#」記号の色は次のパレットを循環して使用します。

```
--accent  --accent2  --p-med  #c08bf0  #5ed3e0  --p-high
```

---

## 3. テーマ

- 既定は **ダーク**。`document.documentElement` の `data-theme` 属性で切り替える（`dark` / `light`）。
- ライトテーマはサーフェス・境界・テキストのみ差し替え、**アクセントと優先度の色は両テーマ共通**。
- テーマ選択は localStorage に永続化される。
- 切り替え時は背景色・文字色が `0.25s` でクロスフェードする。

```css
:root { /* dark トークン */ }
:root[data-theme='light'] { /* surface / border / text を上書き */ }
```

---

## 4. タイポグラフィ

- **フォント**: `Manrope`（Google Fonts, weight 400/500/600/700/800）。
  フォールバックは `system-ui` → 日本語は `Hiragino Kaku Gothic ProN` / `Noto Sans JP`。
- アンチエイリアス: `-webkit-font-smoothing: antialiased`。

| 役割 | size | weight | letter-spacing | 例 |
| --- | --- | --- | --- | --- |
| ページ見出し (h1) | 27px | 800 | -0.025em | 「今日」 |
| ドロワーのタイトル | 19px | 700 | -0.02em | タスク名編集 |
| カレンダー月見出し | 19px | 800 | -0.02em | 「2026年 6月」 |
| ブランド名 | 17px | 800 | -0.02em | Tascal |
| 空状態タイトル | 17px | 700 | – | |
| タスクタイトル | 14.5px | 600 | -0.01em | |
| 入力（追加バー） | 14.5px | 500 | – | |
| 本文・ナビ項目 | 13.5px | 600 | – | |
| メタ情報・チップ | 12〜12.5px | 600〜700 | – | プロジェクト名・締切 |
| セクションラベル | 11px | 700 | 0.08em / **大文字** | 「プロジェクト」「メモ」 |
| カウントピル・タグ | 11.5px | 600〜700 | – | |
| カレンダーの「+N件」 | 10.5px | 700 | – | |

セクションラベル（`--faint` 色・字間広め・`text-transform:uppercase`）は、フォーム/サイドバーの
区切りとして一貫して使用します。

---

## 5. 余白・形状・密度

### 角丸（radius）

| 値 | 用途 |
| --- | --- |
| `--radius` = **14px** | カード・追加バー・進捗カードなど主要な面 |
| 12px | メモ入力欄 |
| 11px | カレンダーのセル・削除ボタン・完了行 |
| 9〜10px | ナビ項目・ボタン・小さめチップ |
| 6〜8px | チェックボックス・カウントピル・タグ |
| 50% | ステータスドット・テーマトグルの点 |

### チェックボックスのサイズ規約

| 文脈 | サイズ | 角丸 |
| --- | --- | --- |
| タスク本体 | 22px | 7px |
| 完了済み行 | 21px | 7px |
| 詳細ドロワーのタイトル横 | 26px | 8px |
| サブタスク | 19px | 6px |

### 密度（row-pad）

タスクカードの上下パディング `--row-pad` を切り替えて密度を変えられます。
`:root` に `data-density` を付与します。

| density | `--row-pad` |
| --- | --- |
| compact | 9px |
| **comfortable（既定）** | 13px |
| roomy | 18px |

### 主要レイアウト寸法

- サイドバー幅: **262px**（固定）
- メインのコンテンツ余白: 左右 **38px**
- 詳細ドロワー幅: **444px**（`max-width: 92vw`）

---

## 6. コンポーネント

### ナビゲーション項目（`.nav-btn`）
スマートリスト・プロジェクト・タグで共通。左にアイコン/色ドット/「#」、右にカウントピル。
- 既定: 文字 `--muted` / 背景 透明
- ホバー: 背景 `--surface`
- 選択（`.is-active`）: 文字 `--text` / 背景 `--surface`、カウントピルが `--surface-2` の塗りになる

### タスクカード（`.card`）
- 背景 `--surface` ＋ `--border`、角丸 `--radius`、出現時に `rowIn` アニメ
- ホバーで境界が `--border-2` に
- 構成: チェックボックス / 優先度ドット＋タイトル / メタ行（プロジェクト・締切・タグ・サブタスク進捗）/ 削除
- 完了時: タイトルが取り消し線＋ `--faint`、チェックは `--accent2` 塗り

### 締切バッジ（`.meta-due`）
状態で色が変わる:
- 期限超過 / 昨日 → `--p-high`（アイコン `⚑`）
- 今日 → `--accent`（アイコン `◉`）
- 明日以降 → `--muted`（アイコン `◷`）

### チップ / ピッカー（詳細ドロワー）
- **picker-chip**（プロジェクト選択）: 選択で `--surface-2` 背景＋`--border-2`＋`--text`
- **seg-chip**（優先度・締切）: 選択でその色の塗り＋前景 `--on-accent`、非選択は `--surface-2`
- **tag-pill**: `--surface-2` の角丸ピル＋削除ボタン

### 追加バー（`.add-bar`）
`--surface` のバー。フォーカス内包（`:focus-within`）で境界が `--accent` に。
右に優先度トグル（クリックで 低→中→高 を循環）と青い「追加」ボタン。

### 達成リング（`.ring`）
SVG の二重円。トラックは半透明、値は `--accent2`。
`stroke-dashoffset` を `0.6s` のイージングでアニメさせ、中央に達成率（%）を表示。

### カレンダー（`.calendar`）
7 列 × 6 週（42 セル）グリッド。当日は枠が `--accent`、日付が `--accent` 塗り。
週末は日曜=`--p-high`／土曜=`--accent`。各セルは最大 3 件のチップ＋「+N件」。

### 詳細ドロワー（`.drawer`）
右からスライドイン（`slideIn`）。背面に半透明オーバーレイ（`fadeIn`）。
ヘッダー（プロジェクト＋閉じる）/ スクロール本体（タイトル・メモ・プロジェクト・優先度・締切・タグ・サブタスク）/
フッター（削除ボタン）の 3 段構成。影は `-26px 0 64px rgba(0,0,0,.32)`。

---

## 7. モーション

| キーフレーム | 用途 | 時間・イージング |
| --- | --- | --- |
| `drawCheck` | チェックマークの線を描く | 0.32s ease-out |
| `burstOut` | 完了時に弾けるパーティクル（7 個） | 0.6s cubic-bezier(.2,.6,.3,1) |
| `rowIn` | タスク行の出現 | 0.2s ease |
| `fadeIn` | 空状態・サブタスク展開・完了リスト・オーバーレイ | 0.18〜0.3s ease |
| `slideIn` | 詳細ドロワーの出現 | 0.26s cubic-bezier(.4,0,.2,1) |
| `pop` | （予備）スケールイン | – |

- ホバー/状態遷移は概ね `0.12〜0.2s`。
- 完了時の祝祭演出（バースト）は設定で無効化可能（実装上の `CELEBRATE` フラグ）。
- `prefers-reduced-motion: reduce` の環境ではアニメーション・トランジションをほぼ無効化する。

---

## 8. アイコン

- **ロゴマーク**: 中心の白丸を、青・緑・黄の 3 衛星と薄いリングが囲むモチーフ（タスクが集まる様子）。
  サイドバーでは放射グラデーションのバッジ上に配置。
- **チェック**: `M4 12.5l5 5L20 6.5` のパス。サイズに応じて線幅 3.2〜3.4 を使い分ける。
- **記号アイコン**: Unicode 文字を使用。
  `◉`（今日）`◷`（予定）`≡`（すべて）`▦`（カレンダー）`⚑`（期限超過）`⌄`（開閉キャレット）`✕`（削除/閉じる）`‹` `›`（月送り）。

---

## 9. アクセシビリティ

- フォーカスリングは `:focus-visible` 時に `--accent` の 2px アウトライン（キーボード操作時のみ表示）。
- アイコンのみのボタン（チェック・削除・閉じる・月送り）には `aria-label` を付与。
- 装飾 SVG は `aria-hidden`。
- ダーク/ライトとも本文テキストは背景に対して十分なコントラストを確保（`--text` / `--muted`）。
- `prefers-reduced-motion` を尊重。

---

## 10. 実装マッピング

| デザイン要素 | 実装ファイル |
| --- | --- |
| トークン（色・角丸・密度・テーマ） | `src/styles/tokens.css` |
| リセット・スクロールバー・キーフレーム | `src/styles/base.css` |
| 全コンポーネントのスタイル | `src/styles/app.css` |
| 色ユーティリティ・パレット | `src/lib/color.ts` |
| 優先度の色・ラベル | `src/lib/priority.ts` |
| 日付・締切表示ロジック | `src/lib/date.ts` |
| 各 UI コンポーネント | `src/components/*` |

> トークンを変更する場合は `tokens.css` を起点に行い、本ドキュメントの表を更新してください。
> 個別コンポーネントに新しい色や寸法を直書きしないことが、デザインシステムを保つ最大のルールです。
